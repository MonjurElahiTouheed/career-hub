
const SectionDescription = ({ children }) => {
    return (
        <p className="font-primary font-medium text-base text-dark-secondary text-center mt-4">{children}</p>
    );
};

export default SectionDescription;